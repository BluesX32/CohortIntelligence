% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomaly.R
\name{run_umap}
\alias{run_umap}
\title{Run UMAP dimensionality reduction on a patient feature matrix}
\usage{
run_umap(
  feature_matrix,
  n_components = 2L,
  n_neighbors = 15L,
  min_dist = 0.1,
  metric = "euclidean",
  random_state = 42L,
  method = c("uwot", "umap", "spectral")
)
}
\arguments{
\item{feature_matrix}{Numeric matrix or tibble with \code{subject_id} column
plus numeric feature columns (from \code{\link[=build_feature_matrix]{build_feature_matrix()}}\verb{$wide}).}

\item{n_components}{Integer. Output dimensions. Default 2.}

\item{n_neighbors}{Integer. UMAP n_neighbors parameter. Default 15.}

\item{min_dist}{Numeric. UMAP min_dist parameter. Default 0.1.}

\item{metric}{Character. Distance metric. Default \code{"euclidean"}.}

\item{random_state}{Integer. Random seed. Default 42.}

\item{method}{Character. \code{"uwot"} (recommended, pure R/C++), \code{"umap"}
(legacy, may require Python), or \code{"spectral"} (pure-R fallback).}
}
\value{
tibble(subject_id, umap_1, umap_2)
}
\description{
Uses \code{uwot::umap()} (pure C++, no Python dependency) as the primary
engine, with a built-in spectral-layout fallback when \code{uwot} is not
installed.
}
