% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank.R
\name{rank_patients}
\alias{rank_patients}
\title{Rank patients by review priority}
\usage{
rank_patients(
  ml_results,
  domain_activity,
  cohort_members,
  weights = list(anomaly = 0.5, cluster_noise = 0.25, sparsity = 0.25),
  n_tiers = 3L
)
}
\arguments{
\item{ml_results}{List from \code{\link[=run_full_ml_pipeline]{run_full_ml_pipeline()}} -- uses \verb{$merged}
tibble (subject_id, cluster_id, anomaly_score). Can also be a plain
tibble(subject_id, anomaly_score, cluster_id).}

\item{domain_activity}{tibble from \code{\link[=build_domain_activity]{build_domain_activity()}}.}

\item{cohort_members}{tibble(subject_id, cohort_start_date).}

\item{weights}{Named list with elements \code{anomaly}, \code{cluster_noise},
\code{sparsity}. Values should sum to 1.}

\item{n_tiers}{Integer. Number of priority tiers to assign. Default 3.}
}
\value{
tibble(subject_id, rank_score, rank_position, priority_tier,
anomaly_score, cluster_id, sparsity_score)
}
\description{
Combines anomaly score (from isolation forest), cluster noise flag
(hdbscan noise points get a bonus), and data sparsity into a weighted
composite score. Patients are ranked descending (rank 1 = highest priority).
}
