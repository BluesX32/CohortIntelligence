% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcomes.R
\name{compute_outcome_labels}
\alias{compute_outcome_labels}
\title{Assign per-patient outcome status and time-to-first-event}
\usage{
compute_outcome_labels(
  cohort_members,
  domain_data,
  domain,
  concept_id = NULL,
  post_index_only = TRUE,
  day_range = c(0L, 730L)
)
}
\arguments{
\item{cohort_members}{tibble(subject_id, cohort_start_date).}

\item{domain_data}{Named list from \code{\link[=extract_omop_domains]{extract_omop_domains()}}.}

\item{domain}{Character(1). Domain to search for the outcome.}

\item{concept_id}{Integer(1). Concept representing the outcome. If \code{NULL}
or \code{NA}, any event in the domain counts.}

\item{post_index_only}{Logical. If \code{TRUE} (default), only events with
\code{days_from_index >= 0} are considered (post-index outcomes).}

\item{day_range}{Integer(2). Further restrict the window. Defaults to
\code{c(0L, 730L)} when \code{post_index_only = TRUE}.}
}
\value{
tibble with columns:
\itemize{
\item \code{subject_id} — all subjects from \code{cohort_members}
\item \code{has_outcome} — logical; \code{TRUE} if at least one event in window
\item \code{days_to_first_event} — integer; \code{NA} when \code{has_outcome == FALSE}
\item \code{days_bin} — ordered factor with levels
\code{"0-90d"}, \code{"91-180d"}, \code{"181-365d"}, \code{">365d"}, \code{"None"}
}
}
\description{
For each patient, determines whether they experienced a given concept
within a specified window relative to the cohort index date.  Returns
an outcome-labelled tibble suitable for overlaying on UMAP plots or
stratifying any downstream analysis.
}
