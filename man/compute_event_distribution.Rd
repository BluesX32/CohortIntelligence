% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcomes.R
\name{compute_event_distribution}
\alias{compute_event_distribution}
\title{Compute the temporal distribution of a concept's events relative to index}
\usage{
compute_event_distribution(
  cohort_members,
  domain_data,
  domain,
  concept_id = NULL,
  day_range = c(-730L, 730L),
  bin_width = 30L
)
}
\arguments{
\item{cohort_members}{tibble(subject_id, cohort_start_date) from
\code{\link[=extract_cohort_members]{extract_cohort_members()}}.}

\item{domain_data}{Named list from \code{\link[=extract_omop_domains]{extract_omop_domains()}}.}

\item{domain}{Character(1). One of \code{"condition"}, \code{"drug"},
\code{"procedure"}, \code{"measurement"}, \code{"observation"}, \code{"visit"},
\code{"death"}.}

\item{concept_id}{Integer(1). Concept to filter on. If \code{NULL} or \code{NA},
all concepts in the domain are included.}

\item{day_range}{Integer(2). Window (days from index) to include.
Default \code{c(-730L, 730L)}.}

\item{bin_width}{Integer(1). Width of each histogram bin in days.
Default \code{30L}.}
}
\value{
tibble with columns: \code{bin_start}, \code{bin_end}, \code{bin_mid}
(all integers, days from index), \code{n_patients} (distinct patients in
bin), \code{n_events} (total event rows in bin).  All bins between
\code{day_range[1]} and \code{day_range[2]} are returned; zero-count bins are
included.
}
\description{
Returns a histogram-ready tibble: how many patients (and events) had a
particular concept recorded in each day-bin relative to the cohort index
date.  Use this to understand \emph{when} a concept of interest typically occurs
in the patient's timeline — before index, shortly after, or much later.
}
