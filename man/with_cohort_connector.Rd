% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{with_cohort_connector}
\alias{with_cohort_connector}
\title{Execute a function within a managed database connection}
\usage{
with_cohort_connector(connector, fn, ...)
}
\arguments{
\item{connector}{A \code{cohort_connector} object.}

\item{fn}{A function accepting the connector as its first argument.}

\item{...}{Additional arguments forwarded to \code{fn}.}
}
\value{
The return value of \code{fn(connector, ...)}.
}
\description{
For \code{cohort_omop_connector}: opens a connection, runs \code{fn(connector)}, and
closes the connection even on error. Stale connections are detected and
reconnected automatically.
}
\details{
For \code{cohort_df_connector}: runs \code{fn(connector)} directly.
}
