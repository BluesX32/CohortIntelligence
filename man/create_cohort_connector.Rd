% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{create_cohort_connector}
\alias{create_cohort_connector}
\title{Wrap a live database connection for use with CohortIntelligence}
\usage{
create_cohort_connector(
  connection,
  cdm_schema,
  vocab_schema = NULL,
  cdm_version = "5.4"
)
}
\arguments{
\item{connection}{A live connection from \code{DatabaseConnector::connect()}.}

\item{cdm_schema}{\code{character(1)}. Schema containing OMOP CDM tables.}

\item{vocab_schema}{\code{character(1)} or \code{NULL}. Vocabulary schema.
Defaults to \code{cdm_schema}.}

\item{cdm_version}{\code{character(1)}. Default \code{"5.4"}.}
}
\value{
An object of class \code{c("cohort_omop_connector", "cohort_connector")}.
}
\description{
The recommended approach. Call \code{DatabaseConnector::connect()} first to
open the connection with the correct permissions, then pass it here.
The connection is reused for all queries and never closed by the package --
the caller controls the lifecycle.
}
