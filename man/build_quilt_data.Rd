% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{build_quilt_data}
\alias{build_quilt_data}
\title{Build the pre-computed data tibble that feeds the reactive quilt plot}
\usage{
build_quilt_data(
  domain_activity,
  rank_df = NULL,
  sort_by = c("cluster", "rank", "subject_id"),
  domains = NULL,
  value_encoding = c("log1p_count", "binary", "count"),
  clip_max = NULL
)
}
\arguments{
\item{domain_activity}{tibble from \code{\link[=build_domain_activity]{build_domain_activity()}}.}

\item{rank_df}{tibble from \code{\link[=rank_patients]{rank_patients()}}. May be \code{NULL}; in that case
patients are sorted by \code{subject_id} and get a placeholder cluster.}

\item{sort_by}{One of \code{"cluster"}, \code{"rank"}, \code{"subject_id"}.}

\item{domains}{Character vector. Restrict to these domains. \code{NULL} = all.}

\item{value_encoding}{\code{"log1p_count"}, \code{"binary"}, or \code{"count"}.}

\item{clip_max}{Numeric. Clip event counts at this value before encoding.
\code{NULL} = no clipping.}
}
\value{
tibble with columns: subject_id, patient_row, display_label,
domain, window_label, window_idx, event_count, fill_value,
cluster_id, priority_tier, rank_position.
}
\description{
Takes domain activity (aggregated event counts per patient/domain/window)
and patient ranking information, and returns a plot-ready tibble with one
row per (subject_id x domain x window_label). The quilt Shiny module caches
this tibble in a \code{reactiveVal} and only filters/re-sorts it interactively.
}
