% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort.R
\name{fetch_cohort_from_json}
\alias{fetch_cohort_from_json}
\title{Instantiate a cohort from an ATLAS JSON file and return cohort members}
\usage{
fetch_cohort_from_json(connector, json_path, cohort_id = 1L)
}
\arguments{
\item{connector}{A \code{cohort_omop_connector} from
\code{\link[=create_cohort_omop_connector]{create_cohort_omop_connector()}}.}

\item{json_path}{Path to an ATLAS cohort definition JSON file.}

\item{cohort_id}{Integer. ID assigned to this cohort in the temp table.
Default \code{1L}.}
}
\value{
tibble(subject_id, cohort_start_date, cohort_end_date)
}
\description{
Reads an ATLAS cohort definition JSON, generates the cohort SQL via
\code{CirceR}, executes it against the CDM into a session-scoped temp table,
and returns the resulting cohort member tibble. No permanent cohort schema
or cohort table is required.
}
